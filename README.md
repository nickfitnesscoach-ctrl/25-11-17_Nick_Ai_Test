# 🤖 AI Lead Magnet Bot

Telegram-бот для учёта калорий с ИИ-агентом для создания персональных планов питания и тренировок.

## 🎯 Основные возможности

- **Учёт калорий** — ежедневный трекинг питания
- **Персональный план** (NEW!) — ИИ-генерация плана питания и тренировок на основе опроса из 10 шагов
- **Типы фигуры** — индивидуальный подбор рекомендаций с визуализацией
- **Часовые пояса** — учёт местного времени пользователя для аналитики

---

## 📋 Требования

- Python 3.11+
- PostgreSQL 14+
- Redis 6+ (опционально, для кеширования; можно использовать MemoryStorage)
- OpenRouter API ключ (для ИИ-агента)

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/ai-lead-magnet-bot.git
cd ai-lead-magnet-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните:
- `TELEGRAM_BOT_TOKEN` — токен вашего бота от [@BotFather](https://t.me/BotFather)
- `DB_*` — параметры подключения к PostgreSQL
- `OPENROUTER_API_KEY` — API ключ от [OpenRouter](https://openrouter.ai/keys)

### 5. Настройка базы данных

#### Создание БД

```bash
# PostgreSQL
createdb calorie_bot_db
```

#### Применение миграций

```bash
# Инициализация Alembic (если первый запуск)
alembic init alembic

# Применение миграций
alembic upgrade head
```

### 6. Запуск бота

```bash
python -m bot
```

---

## 🎨 Подготовка изображений типов фигуры

Перед запуском функции "Персональный план" разместите изображения в:

```
assets/body_types/
├── female/
│   ├── now/
│   │   ├── female_now_1.jpg  (5 изображений)
│   │   └── ...
│   └── ideal/
│       ├── female_ideal_1.jpg  (3 изображения)
│       └── ...
└── male/
    ├── now/
    │   ├── male_now_1.jpg  (4 изображения)
    │   └── ...
    └── ideal/
        ├── male_ideal_1.jpg  (3 изображения)
        └── ...
```

**Рекомендации**:
- Формат: JPEG
- Разрешение: 800×1200 px
- Вес: <200 КБ на файл

Для создания плейсхолдеров на этапе разработки можно использовать скрипт:

```bash
python scripts/generate_placeholders.py
```

---

## ⚙️ Feature Flags

### Включение функции "Персональный план"

В `.env`:
```bash
FEATURE_PERSONAL_PLAN=on  # Включить
FEATURE_PERSONAL_PLAN=off # Выключить (по умолчанию)
```

После изменения флага перезапустите бота.

**Безопасный откат**: При обнаружении проблем просто установите `FEATURE_PERSONAL_PLAN=off` и перезапустите — старый функционал бота останется без изменений.

---

## 📊 Структура проекта

```
.
├── alembic/                # Миграции БД
│   └── versions/
├── assets/                 # Статические файлы
│   └── body_types/         # Изображения типов фигуры
├── bot/
│   ├── handlers/           # Обработчики команд и callback
│   │   ├── common.py
│   │   ├── calories.py
│   │   ├── personal_plan.py  # NEW!
│   │   └── ...
│   ├── states/             # FSM состояния
│   │   └── survey.py       # NEW!
│   ├── keyboards/          # Клавиатуры
│   │   └── survey.py       # NEW!
│   ├── validators/         # Валидаторы ввода
│   │   ├── survey.py       # NEW!
│   │   └── ai_response.py  # NEW!
│   ├── services/
│   │   ├── ai/             # ИИ-сервисы
│   │   │   └── openrouter.py  # NEW!
│   │   ├── database/       # Работа с БД
│   │   └── image_cache.py  # NEW!
│   ├── models/             # SQLAlchemy модели
│   │   ├── user.py
│   │   ├── survey_answer.py  # NEW!
│   │   └── plan.py         # NEW!
│   ├── prompts/            # Промпты для ИИ
│   │   └── personal_plan.py  # NEW!
│   ├── texts/              # Тексты бота
│   │   └── survey.py       # NEW!
│   ├── constants/          # Константы
│   │   └── survey.py       # NEW!
│   ├── utils/              # Утилиты
│   │   └── paths.py        # NEW!
│   ├── config.py           # Конфигурация
│   └── __main__.py         # Точка входа
├── logs/                   # Логи
├── .env                    # Переменные окружения (НЕ коммитить!)
├── .env.example            # Пример .env
├── requirements.txt        # Зависимости
├── roadmap.md              # Детальный план разработки
└── README.md               # Этот файл
```

---

## 🧪 Тестирование

### Ручное тестирование Personal Plan

1. Запустите бота с `FEATURE_PERSONAL_PLAN=on`
2. Отправьте команду `/personal_plan` (или нажмите кнопку в меню)
3. Пройдите опрос:
   - Выберите пол
   - Укажите возраст (14–80)
   - Укажите рост (120–250 см)
   - Укажите вес (30–300 кг)
   - Укажите целевой вес (или пропустите)
   - Выберите уровень активности
   - Выберите текущий тип фигуры (из отдельных изображений)
   - Выберите идеальный тип фигуры
   - Укажите часовой пояс
   - Подтвердите данные
4. Дождитесь генерации плана (~5-10 сек)
5. Проверьте, что в ответе есть:
   - Диапазон калорий вида "≈ X–Y ккал/сут"
   - Фраза "Точные цифры индивидуальны — для точного расчёта обратитесь к тренеру."

### Тестирование возврата назад

- На любом шаге (кроме первого) нажмите "◀️ Назад"
- Проверьте корректность возврата на предыдущий шаг
- Измените данные и пройдите опрос до конца

### Тестирование смены пола

1. Выберите пол "Женский"
2. Пройдите до шага BODY_NOW, выберите вариант
3. Вернитесь назад до шага GENDER
4. Измените пол на "Мужской"
5. Пройдите снова до BODY_NOW
6. Убедитесь, что показываются мужские варианты (4 шт., а не 5)

---

## 🔁 CI/CD

Проект содержит GitHub Actions workflow, который устанавливает зависимости, выводит заглушку для тестов и деплоит приложение на сервер при пуше в `main`. Подробная настройка секретов (`DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY`, `DEPLOY_PATH`) и описание шагов деплоя находятся в [CI_CD.md](CI_CD.md).

---

## 🔧 Разработка

### Создание новой миграции

```bash
alembic revision -m "описание_изменений"
```

### Применение миграций

```bash
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

---

## 📝 Roadmap

Детальный план разработки с архитектурой, тестами и инкрементами находится в [roadmap.md](roadmap.md).

---

## 🤝 Вклад в проект

1. Создайте feature-ветку: `git checkout -b feature/my-feature`
2. Внесите изменения
3. Закоммитьте: `git commit -m "feat: add my feature"`
4. Запушьте: `git push origin feature/my-feature`
5. Создайте Pull Request

---

## 📄 Лицензия

MIT License

---

## 📞 Контакты

Если возникли вопросы или предложения — создайте Issue в репозитории.
